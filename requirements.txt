groq==0.9.0
fastapi==0.111.0
uvicorn==0.30.1
pydantic==2.7.1
schedule==1.2.2
httpx==0.27.0
fpdf2==2.7.9

# RAG + vector search (optional — graceful fallback if not installed)
faiss-cpu==1.8.0
sentence-transformers==3.0.1

# Playwright for browser automation (optional — graceful fallback)
playwright==1.44.0
